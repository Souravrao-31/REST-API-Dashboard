import * as React from 'react';
import { FormSpy } from 'react-final-form';
function Spy(_a) {
    var mutators = _a.form.mutators, values = _a.values;
    var prevValues = React.useRef(values);
    React.useLayoutEffect(function () {
        mutators.resetSubmitErrors({
            current: values,
            prev: prevValues.current
        });
        prevValues.current = values;
    });
    return null;
}
export default function SubmitErrorsSpy() {
    return React.createElement(FormSpy, { subscription: { values: true }, render: Spy });
}
